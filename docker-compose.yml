version: '3'

services:
  mynews:
    build:
      context: .
      dockerfile: Dockerfile
    command: mynews -config /etc/mynews.json
    volumes:
      - type: bind
        source: ./config.json
        target: /etc/mynews.json